(function () {
  "use strict";

  const KEY = "myWallet_v1";
  const $ = (id) => document.getElementById(id);
  const today = () => new Date().toISOString().slice(0, 10);
  const id = () => Date.now().toString(36) + Math.random().toString(36).slice(2, 8);
  const clean = (s) => String(s || "").trim();
  const num = (n) => Math.round((Number(n) || 0) * 100) / 100;

  const I18N = {
    en: {
      home: "Home", banking: "Banking", cards: "Cards", transactions: "Transactions", transfers: "Transfers", goals: "Goals", investments: "Investments", settings: "Settings",
      navHome: "Home", navBanking: "Bank", navCards: "Cards", navTransactions: "Txns", navTransfers: "Move", navGoals: "Goals", navInvestments: "Invest", navSettings: "Settings",
      tagline: "Personal finance command center", morning: "Good Morning", afternoon: "Good Afternoon", evening: "Good Evening",
      walletOverview: "Wallet overview", dashboardCopy: "Track every dollar, reserve money into sinking funds, and watch your net worth grow.",
      netWorth: "Net Worth", freeCash: "Free Cash", reserved: "In Sinking Funds", investmentsTotal: "Investments", recentTransactions: "Recent Transactions", add: "Add",
      commandCenter: "Command Center", commandCopy: "Free cash is your bank balance minus money reserved in sinking funds. That is the number to watch before spending.",
      accountSinking: "Account-centric sinking funds", bankingCopy: "Each account holds real money. Sinking funds reserve pieces of that balance.", account: "Account", fund: "Fund",
      balance: "Balance", available: "Available", fundPct: "Fund %", inside: "Inside", noSinkingFunds: "No sinking funds yet.",
      debtMonitor: "Debt monitor", cardsCopy: "Track balances, limits, APRs, available credit, and quick overrides when you need to reconcile.", creditCard: "Credit Card",
      limit: "Limit", apr: "APR", utilization: "Utilization", edit: "Edit",
      manualLedger: "Manual ledger", transactionsCopy: "Expenses, income, and adjustments update balances automatically.", expense: "Expense", income: "Income", adjustment: "Adjustment",
      moveMoney: "Move money", transfersCopy: "Move money account-to-account, into sinking funds, fund-to-fund, or pay a credit card.", transfer: "Transfer", cardPayment: "Card Payment",
      transferRules: "Transfer Rules", accToAcc: "Account → Account", movesCash: "Moves cash",
      accToFund: "Account → Sinking Fund", reservesCash: "Reserves cash",
      fundToFund: "Fund → Fund", reassignsCash: "Reassigns reserved cash",
      accToCard: "Account/Fund → Credit Card", paysDebt: "Pays debt",
      progress: "Progress from balances", goalsCopy: "Link goals to accounts or sinking funds so progress updates with your ledger.", goal: "Goal",
      current: "Current", targetLabel: "Target", remaining: "Remaining",
      investmentsIntro: "Investment command center", investmentsCopy: "Track institutions, accounts, holdings, allocation, and total portfolio value without brokerage logins.", institution: "Institution", investmentAccount: "Investment Account", holding: "Holding", refreshPrices: "Refresh Prices", lastUpdated: "Last Updated", allocation: "Asset Allocation",
      accountsLabel: "Accounts", holdingsLabel: "Holdings", addInstitutionFirst: "Add an institution first", addAccountFirst: "Add an account first",
      personalize: "Personalize", settingsCopy: "Change your name, language, categories, or export a backup.", firstName: "First name", language: "Language", saveSettings: "Save Settings", exportBackup: "Export Backup", resetApp: "Reset App", categories: "Categories",
      cloudBackup: "Cloud Backup", cloudCopy: "Cloud sync is not connected yet. Local storage and JSON export are active.", status: "Status", notConnected: "Not connected", localExport: "Local export available",
      welcome: "Welcome to My Wallet", setupCopy: "Let’s personalize your command center. You can change this later in Settings.", start: "Start My Wallet", skip: "Skip for now", firstTimeSetup: "First-time setup",
      saved: "Saved", settingsSaved: "Settings saved", delete: "Delete",
      noTransactions: "No transactions yet.", addFirstAccount: "Add your first account.", addFirstCard: "Add your first credit card.", addFirstGoal: "Add your first goal.", addFirstInstitution: "Add your first investment institution.",
      noInvestmentAccounts: "No investment accounts yet.", noHoldings: "No holdings yet.", addHoldingsForAlloc: "Add holdings to see allocation.",
      never: "Never", justNow: "Just now", yesterday: "Yesterday", relHours: "{n}h ago", relDays: "{n}d ago",
      howThisWorks: "How this works",
      helpDashboard: "Dashboard shows net worth, free cash, sinking funds, and investments.",
      helpBanking: "Banking is account-centric. Each account has a balance, sinking funds, and available cash after reserved funds.",
      helpCards: "Credit card balances increase with card expenses and decrease with card payments. You can also edit/override a card balance or available credit.",
      helpTransactions: "Every money change should be entered as a transaction, transfer, or adjustment.",
      helpTransfers: "Transfers move money between accounts, funds, and credit cards without counting as spending.",
      helpGoals: "Goals can link to sinking funds so progress updates automatically.",
      helpInvestments: "Investments are grouped by institution, account, and holding. Prices refresh on app launch or when you tap Refresh Prices.",
      resetConfirm: "Reset My Wallet data on this device?", deleteHoldingConfirm: "Delete this holding?", holdingDeleted: "Holding deleted",
      priceUpdated1: "holding updated", priceUpdatedN: "holdings updated", noPricesUpdated: "No prices updated",
      modalAddAccount: "Add Account", modalAddFund: "Add Sinking Fund", modalAddCard: "Add Credit Card", modalEditCard: "Edit / Override Card",
      modalAddExpense: "Add Expense", modalAddIncome: "Income", modalAdjustment: "Balance Adjustment", modalPayment: "Credit Card Payment",
      modalAddGoal: "Add Goal", modalAddCategory: "Add Category", modalAddInstitution: "Add Institution", modalAddInvestAccount: "Add Investment Account", modalAddHolding: "Add Holding",
      fieldName: "Name", fieldType: "Type", fieldOpeningBalance: "Opening balance", fieldAccount: "Account",
      fieldFundName: "Fund name", fieldReservedAmount: "Reserved amount",
      fieldCardName: "Card name", fieldBalanceOwed: "Balance owed", fieldCreditLimit: "Credit limit",
      fieldAvailOverride: "Available credit override", fieldInterestRate: "Interest rate / APR %",
      fieldCardHelp: "Use available credit override if you know available credit better than the balance. The app will calculate balance = limit - available.",
      fieldAccountCard: "Account/Card", fieldMerchant: "Merchant", fieldCategory: "Category", fieldAmount: "Amount",
      fieldDepositTo: "Deposit to", fieldSource: "Source", fieldNote: "Note", fieldNewBalance: "New balance",
      fieldFrom: "From", fieldTo: "To",
      fieldGoalName: "Goal name", fieldTarget: "Target", fieldLinkToFund: "Link to fund", fieldNone: "None",
      fieldInstitution: "Institution", fieldAccountName: "Account name", fieldAccountType: "Account type",
      fieldTicker: "Ticker / Symbol", fieldHoldingName: "Holding name", fieldAssetType: "Asset type",
      fieldValueMethod: "Value method", fieldShares: "Shares / coins", sharesMethod: "Shares / coins x price", balanceMethod: "Balance directly",
      fieldPrice: "Price", fieldBalance: "Balance", fieldCategoryName: "Category name", fieldCreditCard: "Credit card",
    },
    es: {
      home: "Inicio", banking: "Bancos", cards: "Tarjetas", transactions: "Transacciones", transfers: "Traspasos", goals: "Metas", investments: "Inversiones", settings: "Ajustes",
      navHome: "Inicio", navBanking: "Banco", navCards: "Tarjetas", navTransactions: "Transacciones", navTransfers: "Mover", navGoals: "Metas", navInvestments: "Invertir", navSettings: "Ajustes",
      tagline: "Centro de control financiero", morning: "Buenos Días", afternoon: "Buenas Tardes", evening: "Buenas Noches",
      walletOverview: "Resumen de cartera", dashboardCopy: "Controla cada dólar, separa dinero en fondos, y mira crecer tu patrimonio.",
      netWorth: "Patrimonio Neto", freeCash: "Efectivo Libre", reserved: "En Fondos", investmentsTotal: "Inversiones", recentTransactions: "Transacciones Recientes", add: "Agregar",
      commandCenter: "Centro de Control", commandCopy: "El efectivo libre es el balance bancario menos lo reservado en fondos. Ese es el número para revisar antes de gastar.",
      accountSinking: "Fondos dentro de cuentas", bankingCopy: "Cada cuenta tiene dinero real. Los fondos separan partes de ese balance.", account: "Cuenta", fund: "Fondo",
      balance: "Balance", available: "Disponible", fundPct: "% Fondos", inside: "Dentro de", noSinkingFunds: "Sin fondos aún.",
      debtMonitor: "Monitor de deuda", cardsCopy: "Controla balances, límites, APR, crédito disponible y ajustes rápidos para reconciliar.", creditCard: "Tarjeta",
      limit: "Límite", apr: "APR", utilization: "Uso", edit: "Editar",
      manualLedger: "Registro manual", transactionsCopy: "Gastos, ingresos y ajustes actualizan los balances automáticamente.", expense: "Gasto", income: "Ingreso", adjustment: "Ajuste",
      moveMoney: "Mover dinero", transfersCopy: "Mueve dinero entre cuentas, fondos, o para pagar una tarjeta.", transfer: "Traspaso", cardPayment: "Pago de Tarjeta",
      transferRules: "Reglas de Traspaso", accToAcc: "Cuenta → Cuenta", movesCash: "Mueve efectivo",
      accToFund: "Cuenta → Fondo", reservesCash: "Reserva efectivo",
      fundToFund: "Fondo → Fondo", reassignsCash: "Reasigna efectivo reservado",
      accToCard: "Cuenta/Fondo → Tarjeta", paysDebt: "Paga deuda",
      progress: "Progreso por balances", goalsCopy: "Conecta metas a cuentas o fondos para que el progreso se actualice solo.", goal: "Meta",
      current: "Actual", targetLabel: "Objetivo", remaining: "Restante",
      investmentsIntro: "Centro de inversiones", investmentsCopy: "Controla instituciones, cuentas, inversiones, asignación y valor total sin conectar casas de corretaje.", institution: "Institución", investmentAccount: "Cuenta de inversión", holding: "Inversión", refreshPrices: "Actualizar precios", lastUpdated: "Actualizado", allocation: "Asignación",
      accountsLabel: "Cuentas", holdingsLabel: "Inversiones", addInstitutionFirst: "Primero agrega una institución", addAccountFirst: "Primero agrega una cuenta",
      personalize: "Personalizar", settingsCopy: "Cambia tu nombre, idioma, categorías o exporta un respaldo.", firstName: "Nombre", language: "Idioma", saveSettings: "Guardar", exportBackup: "Exportar", resetApp: "Reiniciar", categories: "Categorías",
      cloudBackup: "Respaldo en la nube", cloudCopy: "La sincronización en la nube no está conectada. El almacenamiento local y exportación JSON están activos.", status: "Estado", notConnected: "No conectado", localExport: "Exportación local disponible",
      welcome: "Bienvenido a My Wallet", setupCopy: "Vamos a personalizar tu centro de control. Puedes cambiar esto luego en Ajustes.", start: "Comenzar", skip: "Omitir por ahora", firstTimeSetup: "Configuración inicial",
      saved: "Guardado", settingsSaved: "Ajustes guardados", delete: "Eliminar",
      noTransactions: "Sin transacciones aún.", addFirstAccount: "Agrega tu primera cuenta.", addFirstCard: "Agrega tu primera tarjeta.", addFirstGoal: "Agrega tu primera meta.", addFirstInstitution: "Agrega tu primera institución de inversión.",
      noInvestmentAccounts: "Sin cuentas de inversión aún.", noHoldings: "Sin inversiones aún.", addHoldingsForAlloc: "Agrega inversiones para ver la asignación.",
      never: "Nunca", justNow: "Ahora mismo", yesterday: "Ayer", relHours: "hace {n}h", relDays: "hace {n}d",
      howThisWorks: "Cómo funciona",
      helpDashboard: "El panel muestra patrimonio neto, efectivo libre, fondos e inversiones.",
      helpBanking: "La banca está centrada en cuentas. Cada cuenta tiene balance, fondos y efectivo disponible después de los fondos reservados.",
      helpCards: "Los balances de tarjetas aumentan con gastos y disminuyen con pagos. También puedes editar el balance o crédito disponible.",
      helpTransactions: "Cada cambio de dinero debe ingresarse como transacción, traspaso o ajuste.",
      helpTransfers: "Los traspasos mueven dinero entre cuentas, fondos y tarjetas sin contar como gasto.",
      helpGoals: "Las metas pueden vincularse a fondos para que el progreso se actualice automáticamente.",
      helpInvestments: "Las inversiones se agrupan por institución, cuenta e inversión. Los precios se actualizan al abrir la app o al tocar Actualizar precios.",
      resetConfirm: "¿Reiniciar los datos de My Wallet en este dispositivo?", deleteHoldingConfirm: "¿Eliminar esta inversión?", holdingDeleted: "Inversión eliminada",
      priceUpdated1: "inversión actualizada", priceUpdatedN: "inversiones actualizadas", noPricesUpdated: "Sin precios actualizados",
      modalAddAccount: "Agregar Cuenta", modalAddFund: "Agregar Fondo", modalAddCard: "Agregar Tarjeta", modalEditCard: "Editar Tarjeta",
      modalAddExpense: "Agregar Gasto", modalAddIncome: "Ingreso", modalAdjustment: "Ajuste de Balance", modalPayment: "Pago de Tarjeta",
      modalAddGoal: "Agregar Meta", modalAddCategory: "Agregar Categoría", modalAddInstitution: "Agregar Institución", modalAddInvestAccount: "Agregar Cuenta de Inversión", modalAddHolding: "Agregar Inversión",
      fieldName: "Nombre", fieldType: "Tipo", fieldOpeningBalance: "Balance inicial", fieldAccount: "Cuenta",
      fieldFundName: "Nombre del fondo", fieldReservedAmount: "Monto reservado",
      fieldCardName: "Nombre de tarjeta", fieldBalanceOwed: "Balance adeudado", fieldCreditLimit: "Límite de crédito",
      fieldAvailOverride: "Crédito disponible (manual)", fieldInterestRate: "Tasa de interés / APR %",
      fieldCardHelp: "Usa el crédito disponible manual si lo conoces. La app calculará balance = límite - disponible.",
      fieldAccountCard: "Cuenta/Tarjeta", fieldMerchant: "Comercio", fieldCategory: "Categoría", fieldAmount: "Monto",
      fieldDepositTo: "Depositar en", fieldSource: "Fuente", fieldNote: "Nota", fieldNewBalance: "Nuevo balance",
      fieldFrom: "De", fieldTo: "A",
      fieldGoalName: "Nombre de meta", fieldTarget: "Objetivo", fieldLinkToFund: "Vincular a fondo", fieldNone: "Ninguno",
      fieldInstitution: "Institución", fieldAccountName: "Nombre de cuenta", fieldAccountType: "Tipo de cuenta",
      fieldTicker: "Ticker / Símbolo", fieldHoldingName: "Nombre de inversión", fieldAssetType: "Tipo de activo",
      fieldValueMethod: "Método de valor", fieldShares: "Acciones / monedas", sharesMethod: "Acciones / monedas x precio", balanceMethod: "Balance directo",
      fieldPrice: "Precio", fieldBalance: "Balance", fieldCategoryName: "Nombre de categoría", fieldCreditCard: "Tarjeta de crédito",
    }
  };

  let activeView = "dashboard";
  let state = load();
  const t = (key) => (I18N[state.settings.lang || "en"] || I18N.en)[key] || I18N.en[key] || key;
  const money = (n) => (Number(n) || 0).toLocaleString(state.settings.lang === "es" ? "es-US" : "en-US", { style: "currency", currency: "USD" });

  function starterState() {
    return {
      version: "my-wallet-1.1",
      setupComplete: false,
      settings: { firstName: "", lang: "en", lastBackup: "", lastInvestmentRefresh: "" },
      accounts: [
        { id: id(), name: "SoFi Checking", type: "checking", balance: 0, funds: [] },
        { id: id(), name: "SoFi Savings", type: "savings", balance: 0, funds: [
          { id: id(), name: "Emergency Fund", amount: 0 },
          { id: id(), name: "Car Maintenance", amount: 0 },
        ] },
      ],
      cards: [], categories: ["Dining", "Groceries", "Gas", "Bills", "Shopping", "Health", "Travel", "Income", "Other"], transactions: [], goals: [],
      investmentInstitutions: [],
      investmentAccounts: [],
      investmentHoldings: [],
    };
  }

  function load() { try { const raw = localStorage.getItem(KEY); if (raw) return normalize(JSON.parse(raw)); } catch (err) { console.warn(err); } return starterState(); }
  function normalize(raw) {
    const base = starterState();
    return { ...base, ...raw, settings: { ...base.settings, ...(raw.settings || {}) }, accounts: Array.isArray(raw.accounts) ? raw.accounts : base.accounts, cards: Array.isArray(raw.cards) ? raw.cards.map(c => ({ apr: 0, ...c })) : [], categories: Array.isArray(raw.categories) ? raw.categories : base.categories, transactions: Array.isArray(raw.transactions) ? raw.transactions : [], goals: Array.isArray(raw.goals) ? raw.goals : [], investmentInstitutions: Array.isArray(raw.investmentInstitutions) ? raw.investmentInstitutions : [], investmentAccounts: Array.isArray(raw.investmentAccounts) ? raw.investmentAccounts : [], investmentHoldings: Array.isArray(raw.investmentHoldings) ? raw.investmentHoldings.map(h => ({ ...h, value: holdingValue(h) })) : [] };
  }
  function save(next) { state = normalize(next); localStorage.setItem(KEY, JSON.stringify(state)); applyTheme(); render(); }
  function toast(message) { const el = $("toast"); el.textContent = message; el.classList.add("show"); clearTimeout(toast.timer); toast.timer = setTimeout(() => el.classList.remove("show"), 2200); }
  function applyTheme() { document.documentElement.lang = state.settings.lang || "en"; const tag = document.querySelector("[data-i18n='tagline']"); if (tag) tag.textContent = t("tagline"); document.querySelectorAll("[data-label]").forEach(el => { el.textContent = t(el.dataset.label); }); document.querySelectorAll("[data-nav-label]").forEach(el => { el.textContent = t(el.dataset.navLabel); }); }
  function greeting() { const h = new Date().getHours(); const part = h < 12 ? t("morning") : h < 17 ? t("afternoon") : t("evening"); return state.settings.firstName ? `${part}, ${state.settings.firstName}` : "My Wallet"; }
  function totals() { const bank = state.accounts.reduce((s, a) => s + num(a.balance), 0); const reserved = state.accounts.reduce((s, a) => s + (a.funds || []).reduce((x, f) => x + num(f.amount), 0), 0); const cardDebt = state.cards.reduce((s, c) => s + num(c.balance), 0); const cardLimit = state.cards.reduce((s, c) => s + num(c.limit), 0); const investments = investmentTotal(); return { bank, reserved, free: bank - reserved, investments, cardDebt, cardLimit, netWorth: bank + investments - cardDebt }; }
  function accountReserved(account) { return (account.funds || []).reduce((s, f) => s + num(f.amount), 0); }
  function accountAvailable(account) { return num(account.balance) - accountReserved(account); }
  function cardAvailable(card) { return Math.max(0, num(card.limit) - num(card.balance)); }
  function holdingValue(h) { return num(h && h.valueMode === "balance" ? h.balance : num(h?.shares) * num(h?.price)); }
  function investmentTotal() { return state.investmentHoldings.reduce((s, h) => s + holdingValue(h), 0); }
  function holdingsForAccount(accountId) { return state.investmentHoldings.filter((h) => h.accountId === accountId); }
  function institutionName(institutionId) { return state.investmentInstitutions.find((i) => i.id === institutionId)?.name || "Institution"; }
  function investmentAccountValue(accountId) { return holdingsForAccount(accountId).reduce((s, h) => s + holdingValue(h), 0); }
  function assetBucket(assetType) { const type = String(assetType || "").toLowerCase(); if (/bond|treasury|cd/.test(type)) return "Bonds"; if (/cash|money/.test(type)) return "Cash"; if (/crypto/.test(type)) return "Crypto"; return "Stocks"; }
  function relativeTime(value) { if (!value) return t("never"); const ms = Date.now() - new Date(value).getTime(); if (!Number.isFinite(ms)) return t("never"); const hours = Math.max(0, Math.round(ms / 3600000)); if (hours < 1) return t("justNow"); if (hours < 24) return t("relHours").replace("{n}", hours); const days = Math.round(hours / 24); return days === 1 ? t("yesterday") : t("relDays").replace("{n}", days); }

  function render() { $("greeting").textContent = greeting(); document.querySelectorAll(".tab").forEach((b) => b.classList.toggle("active", b.dataset.view === activeView)); const view = $("main"); view.innerHTML = screens[activeView] ? screens[activeView]() : screens.dashboard(); wire(view); if (!state.setupComplete) renderOnboarding(); }

  const screens = {
    dashboard() { const x = totals(); const recent = state.transactions.slice(0, 6); return `<section class="hero wide"><div class="eyebrow">${t("walletOverview")}</div><h2>${greeting()}</h2><p>${t("dashboardCopy")}</p></section><section class="kpi-grid wide">${kpi(t("netWorth"), money(x.netWorth), x.netWorth >= 0 ? "good" : "bad")}${kpi(t("freeCash"), money(x.free), x.free >= 0 ? "good" : "bad")}${kpi(t("reserved"), money(x.reserved), "gold")}${kpi(t("investmentsTotal"), money(x.investments), "good")}</section><section class="card"><div class="section-title"><h2>${t("recentTransactions")}</h2><button class="btn secondary" data-view-go="transactions">${t("add")}</button></div>${recent.length ? recent.map(transactionRow).join("") : empty(t("noTransactions"))}</section><section class="card"><div class="section-title"><h2>${t("commandCenter")}</h2></div><p class="help-text">${t("commandCopy")}</p></section>`; },
    banking() { return `<section class="hero wide"><div class="eyebrow">${t("accountSinking")}</div><h2>${t("banking")}</h2><p>${t("bankingCopy")}</p><div class="button-row"><button class="btn gold" data-modal="account">+ ${t("account")}</button><button class="btn secondary" data-modal="fund">+ ${t("fund")}</button></div></section>${state.accounts.length ? state.accounts.map(accountCard).join("") : empty(t("addFirstAccount"))}`; },
    cards() { return `<section class="hero wide"><div class="eyebrow">${t("debtMonitor")}</div><h2>${t("cards")}</h2><p>${t("cardsCopy")}</p><div class="button-row"><button class="btn gold" data-modal="card">+ ${t("creditCard")}</button></div></section>${state.cards.length ? state.cards.map(cardCard).join("") : empty(t("addFirstCard"))}`; },
    transactions() { return `<section class="hero wide"><div class="eyebrow">${t("manualLedger")}</div><h2>${t("transactions")}</h2><p>${t("transactionsCopy")}</p><div class="button-row"><button class="btn gold" data-modal="expense">+ ${t("expense")}</button><button class="btn secondary" data-modal="income">+ ${t("income")}</button><button class="btn secondary" data-modal="adjustment">${t("adjustment")}</button></div></section><section class="card wide">${state.transactions.length ? state.transactions.map(transactionRow).join("") : empty(t("noTransactions"))}</section>`; },
    transfers() { return `<section class="hero wide"><div class="eyebrow">${t("moveMoney")}</div><h2>${t("transfers")}</h2><p>${t("transfersCopy")}</p><div class="button-row"><button class="btn gold" data-modal="transfer">+ ${t("transfer")}</button><button class="btn secondary" data-modal="payment">${t("cardPayment")}</button></div></section><section class="card wide"><h2>${t("transferRules")}</h2><div class="row"><span>${t("accToAcc")}</span><strong>${t("movesCash")}</strong></div><div class="row"><span>${t("accToFund")}</span><strong>${t("reservesCash")}</strong></div><div class="row"><span>${t("fundToFund")}</span><strong>${t("reassignsCash")}</strong></div><div class="row"><span>${t("accToCard")}</span><strong>${t("paysDebt")}</strong></div></section>`; },
    goals() { return `<section class="hero wide"><div class="eyebrow">${t("progress")}</div><h2>${t("goals")}</h2><p>${t("goalsCopy")}</p><div class="button-row"><button class="btn gold" data-modal="goal">+ ${t("goal")}</button></div></section>${state.goals.length ? state.goals.map(goalCard).join("") : empty(t("addFirstGoal"))}`; },
    investments() { const total = investmentTotal(); return `<section class="hero wide"><div class="eyebrow">${t("investmentsIntro")}</div><h2>${t("investments")}</h2><p>${t("investmentsCopy")}</p><div class="button-row"><button class="btn gold" data-modal="institution">+ ${t("institution")}</button><button class="btn secondary" data-modal="investmentAccount">+ ${t("investmentAccount")}</button><button class="btn secondary" data-modal="holding">+ ${t("holding")}</button><button class="btn secondary" data-action="refresh-prices">${t("refreshPrices")}</button></div></section><section class="kpi-grid wide">${kpi(t("investmentsTotal"), money(total), "good")}${kpi(t("lastUpdated"), relativeTime(state.settings.lastInvestmentRefresh), "gold")}${kpi(t("accountsLabel"), String(state.investmentAccounts.length), "")}${kpi(t("holdingsLabel"), String(state.investmentHoldings.length), "")}</section>${allocationCard()}${state.investmentInstitutions.length ? state.investmentInstitutions.map(institutionCard).join("") : empty(t("addFirstInstitution"))}`; },
    settings() { return `<section class="hero wide"><div class="eyebrow">${t("personalize")}</div><h2>${t("settings")}</h2><p>${t("settingsCopy")}</p></section><section class="card wide"><div class="form-grid"><div class="field"><label>${t("firstName")}</label><input id="setting-name" value="${escapeAttr(state.settings.firstName)}"></div><div class="field"><label>${t("language")}</label><select id="setting-lang"><option value="en" ${state.settings.lang !== "es" ? "selected" : ""}>English</option><option value="es" ${state.settings.lang === "es" ? "selected" : ""}>Español</option></select></div><button class="btn gold" type="button" data-action="save-settings">${t("saveSettings")}</button><button class="btn secondary" type="button" data-action="export">${t("exportBackup")}</button><button class="btn danger" type="button" data-action="reset">${t("resetApp")}</button></div></section><section class="card wide"><div class="section-title"><h2>${t("cloudBackup")}</h2></div><p class="help-text">${t("cloudCopy")}</p><div class="row"><span>${t("status")}</span><strong class="bad">${t("notConnected")}</strong></div><div class="row"><span>${t("exportBackup")}</span><strong class="good">${t("localExport")}</strong></div></section><section class="card wide"><div class="section-title"><h2>${t("categories")}</h2><button class="btn secondary" data-modal="category">+ ${t("add")}</button></div>${state.categories.map((c) => `<span class="fund-pill"><b>${esc(c)}</b></span>`).join("")}</section>`; },
  };

  function kpi(label, value, tone) { return `<div class="kpi ${tone || ""}"><span>${label}</span><strong>${value}</strong></div>`; }
  function empty(text) { return `<div class="empty">${text}</div>`; }
  function esc(s) { return String(s ?? "").replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c])); }
  function escapeAttr(s) { return esc(s).replace(/"/g, "&quot;"); }
  function accountCard(a) { const reserved = accountReserved(a), available = num(a.balance) - reserved, pct = num(a.balance) ? Math.min(100, (reserved / num(a.balance)) * 100) : 0; return `<section class="card account-card"><div class="section-title"><div><h2>${esc(a.name)}</h2><p class="muted">${esc(a.type)}</p></div><button class="btn secondary" data-modal="fund" data-account="${a.id}">+ ${t("fund")}</button></div><div class="kpi-grid"><div class="kpi"><span>${t("balance")}</span><strong>${money(a.balance)}</strong></div><div class="kpi gold"><span>${t("reserved")}</span><strong>${money(reserved)}</strong></div><div class="kpi ${available >= 0 ? "good" : "bad"}"><span>${t("available")}</span><strong>${money(available)}</strong></div><div class="kpi"><span>${t("fundPct")}</span><strong>${pct.toFixed(0)}%</strong></div></div><div class="meter"><span style="width:${pct}%"></span></div>${(a.funds || []).map((f) => `<div class="fund-pill"><div><b>${esc(f.name)}</b><small>${t("inside")} ${esc(a.name)}</small></div><strong>${money(f.amount)}</strong></div>`).join("") || `<p class="help-text">${t("noSinkingFunds")}</p>`}</section>`; }
  function cardCard(c) { const used = num(c.limit) ? (num(c.balance) / num(c.limit)) * 100 : 0; return `<section class="card"><div class="section-title"><h2>${esc(c.name)}</h2><button class="btn secondary" data-modal="editCard" data-card="${c.id}">${t("edit")}</button></div><div class="row"><span>${t("balance")}</span><strong>${money(c.balance)}</strong></div><div class="row"><span>${t("limit")}</span><strong>${money(c.limit)}</strong></div><div class="row"><span>${t("available")}</span><strong class="good">${money(cardAvailable(c))}</strong></div><div class="row"><span>${t("apr")}</span><strong>${num(c.apr).toFixed(2)}%</strong></div><div class="row"><span>${t("utilization")}</span><strong>${used.toFixed(0)}%</strong></div><div class="meter"><span style="width:${Math.min(100, used)}%"></span></div></section>`; }
  function transactionRow(tx) { return `<div class="row"><span><b>${esc(tx.merchant || tx.note || tx.type)}</b><br><small>${esc(tx.date)} · ${esc(tx.category || tx.type)}</small></span><strong class="amount ${tx.amount < 0 ? "bad" : ""}">${money(Math.abs(tx.amount))}</strong></div>`; }
  function goalCard(g) { const current = goalCurrent(g), pct = num(g.target) ? Math.min(100, (current / num(g.target)) * 100) : 0; return `<section class="card"><div class="section-title"><h2>${esc(g.name)}</h2><strong>${pct.toFixed(0)}%</strong></div><div class="row"><span>${t("current")}</span><strong>${money(current)}</strong></div><div class="row"><span>${t("targetLabel")}</span><strong>${money(g.target)}</strong></div><div class="row"><span>${t("remaining")}</span><strong>${money(Math.max(0, num(g.target) - current))}</strong></div><div class="meter"><span style="width:${pct}%"></span></div></section>`; }
  function allocationCard() { const total = investmentTotal(); const buckets = state.investmentHoldings.reduce((map, h) => { const key = assetBucket(h.assetType); map[key] = (map[key] || 0) + holdingValue(h); return map; }, {}); const rows = ["Stocks", "Bonds", "Cash", "Crypto"].map((key) => { const value = buckets[key] || 0; const pct = total ? (value / total) * 100 : 0; return `<div class="row"><span>${key}</span><strong>${money(value)} - ${pct.toFixed(0)}%</strong></div><div class="meter small"><span style="width:${Math.min(100, pct)}%"></span></div>`; }).join(""); return `<section class="card wide"><div class="section-title"><h2>${t("allocation")}</h2></div>${total ? rows : empty(t("addHoldingsForAlloc"))}</section>`; }
  function institutionCard(inst) { const accounts = state.investmentAccounts.filter((a) => a.institutionId === inst.id); const total = accounts.reduce((s, a) => s + investmentAccountValue(a.id), 0); return `<section class="card wide"><div class="section-title"><div><h2>${esc(inst.name)}</h2><p class="muted">${money(total)} - ${accounts.length} ${accounts.length === 1 ? t("account").toLowerCase() : t("accountsLabel").toLowerCase()}</p></div><button class="btn secondary" data-modal="investmentAccount" data-institution="${inst.id}">+ ${t("investmentAccount")}</button></div>${accounts.length ? accounts.map(investmentAccountCard).join("") : `<p class="help-text">${t("noInvestmentAccounts")}</p>`}</section>`; }
  function investmentAccountCard(account) { const holdings = holdingsForAccount(account.id); const value = investmentAccountValue(account.id); return `<div class="invest-account"><div class="section-title compact"><div><h3>${esc(account.name)}</h3><p class="muted">${esc(account.accountType)} - ${money(value)}</p></div><button class="btn secondary" data-modal="holding" data-invest-account="${account.id}">+ ${t("holding")}</button></div>${holdings.length ? holdings.map(holdingRow).join("") : `<p class="help-text">${t("noHoldings")}</p>`}</div>`; }
  function holdingRow(h) { const value = holdingValue(h); const detail = h.valueMode === "balance" ? `${esc(h.assetType)} - balance` : `${esc(h.ticker || h.name)} - ${num(h.shares)} x ${money(h.price)}`; return `<div class="holding-row"><div><b>${esc(h.name || h.ticker || "Holding")}</b><small>${detail}</small><small>${t("lastUpdated")}: ${relativeTime(h.lastUpdated)}</small></div><div><strong>${money(value)}</strong><div class="button-row mini-actions"><button class="btn secondary" data-modal="holding" data-holding="${h.id}">${t("edit")}</button><button class="btn danger" data-action="delete-holding" data-holding="${h.id}">${t("delete")}</button></div></div></div>`; }
  function goalCurrent(goal) { const allFunds = state.accounts.flatMap((a) => (a.funds || []).map((f) => ({ ...f, accountId: a.id }))); const fund = allFunds.find((f) => f.id === goal.linkedFundId); if (fund) return num(fund.amount); const acc = state.accounts.find((a) => a.id === goal.linkedAccountId); return acc ? num(acc.balance) : 0; }

  function wire(root) { root.querySelectorAll("[data-view-go]").forEach((b) => b.onclick = () => switchView(b.dataset.viewGo)); root.querySelectorAll("[data-modal]").forEach((b) => b.onclick = () => openModal(b.dataset.modal, { accountId: b.dataset.account, cardId: b.dataset.card, institutionId: b.dataset.institution, investmentAccountId: b.dataset.investAccount, holdingId: b.dataset.holding })); root.querySelectorAll("[data-help]").forEach((b) => b.onclick = () => help(b.dataset.help)); root.querySelectorAll("[data-action]").forEach((b) => b.onclick = () => actions[b.dataset.action] && actions[b.dataset.action](b)); }
  function switchView(view) { activeView = view; render(); $("main").focus(); }
  document.querySelectorAll(".tab").forEach((b) => b.onclick = () => switchView(b.dataset.view)); $("help-btn").onclick = () => help(activeView);
  const actions = { "save-settings"() { const next = clone(); next.settings.firstName = clean($("setting-name").value); next.settings.lang = $("setting-lang").value; save(next); toast(t("settingsSaved")); }, export() { const blob = new Blob([JSON.stringify(state, null, 2)], { type: "application/json" }); const a = document.createElement("a"); a.href = URL.createObjectURL(blob); a.download = `my-wallet-backup-${today()}.json`; a.click(); }, reset() { if (confirm(t("resetConfirm"))) { localStorage.removeItem(KEY); state = starterState(); applyTheme(); render(); } }, "delete-holding"(button) { if (!confirm(t("deleteHoldingConfirm"))) return; const next = clone(); next.investmentHoldings = next.investmentHoldings.filter((h) => h.id !== button.dataset.holding); save(next); toast(t("holdingDeleted")); }, "refresh-prices"() { refreshInvestmentPrices(false); } };
  function clone() { return JSON.parse(JSON.stringify(state)); }
  function accountOptions(selected) { return state.accounts.map((a) => `<option value="${a.id}" ${a.id === selected ? "selected" : ""}>${esc(a.name)}</option>`).join(""); }
  function cardOptions() { return state.cards.map((c) => `<option value="${c.id}">${esc(c.name)}</option>`).join(""); }
  function fundOptions() { return state.accounts.flatMap((a) => (a.funds || []).map((f) => `<option value="${a.id}:${f.id}">${esc(a.name)} · ${esc(f.name)}</option>`)).join(""); }
  function categoryOptions() { return state.categories.map((c) => `<option>${esc(c)}</option>`).join(""); }

  function closeModal(){ const m=$("modal"); m.classList.add("hidden"); m.innerHTML=""; }
  function investmentInstitutionOptions(selected) { return (state.investmentInstitutions.length ? state.investmentInstitutions : [{ id: "", name: t("addInstitutionFirst") }]).map((i) => `<option value="${i.id}" ${i.id === selected ? "selected" : ""}>${esc(i.name)}</option>`).join(""); }
  function investmentAccountOptions(selected) { return (state.investmentAccounts.length ? state.investmentAccounts : [{ id: "", name: t("addAccountFirst"), institutionId: "" }]).map((a) => `<option value="${a.id}" ${a.id === selected ? "selected" : ""}>${esc(institutionName(a.institutionId))} - ${esc(a.name)}</option>`).join(""); }
  function investmentAccountTypeOptions(selected) { return ["Taxable Brokerage", "Roth IRA", "Traditional IRA", "401(k)", "Roth 401(k)", "HSA", "529", "Trust Account", "Cash Management", "Other"].map((x) => `<option ${x === selected ? "selected" : ""}>${x}</option>`).join(""); }
  function assetTypeOptions(selected) { return ["Stock", "ETF", "Mutual Fund", "Bond Fund", "Treasury", "Money Market", "CD", "Cash", "Crypto", "Other"].map((x) => `<option ${x === selected ? "selected" : ""}>${x}</option>`).join(""); }
  function openModal(type, options) { options = options || {}; const modal = $("modal"); modal.innerHTML = `<div class="modal-card"><div class="section-title"><h2>${modalTitle(type)}</h2><button class="icon-btn" data-close>x</button></div>${modalBody(type, options)}</div>`; modal.classList.remove("hidden"); modal.setAttribute("aria-hidden", "false"); modal.querySelector("[data-close]").onclick = closeModal; const form = modal.querySelector("form"); if (form) form.onsubmit = (e) => { e.preventDefault(); handleForm(type, new FormData(form)); }; }
  function modalTitle(type){ return ({ account:t("modalAddAccount"), fund:t("modalAddFund"), card:t("modalAddCard"), editCard:t("modalEditCard"), expense:t("modalAddExpense"), income:t("modalAddIncome"), adjustment:t("modalAdjustment"), transfer:t("transfer"), payment:t("modalPayment"), goal:t("modalAddGoal"), category:t("modalAddCategory"), institution:t("modalAddInstitution"), investmentAccount:t("modalAddInvestAccount"), holding:t("modalAddHolding") })[type] || t("add"); }
  function modalBody(type, options) { const presetAccountId = options.accountId; const card = state.cards.find(c => c.id === options.cardId) || {}; const holding = state.investmentHoldings.find(h => h.id === options.holdingId) || {}; if (type === "account") return form(`<div class="field"><label>${t("fieldName")}</label><input name="name" required placeholder="SoFi Savings"></div><div class="field"><label>${t("fieldType")}</label><select name="type"><option>checking</option><option>savings</option><option>cash</option></select></div><div class="field"><label>${t("fieldOpeningBalance")}</label><input name="balance" type="number" step="0.01" value="0"></div>`); if (type === "fund") return form(`<div class="field"><label>${t("fieldAccount")}</label><select name="accountId">${accountOptions(presetAccountId)}</select></div><div class="field"><label>${t("fieldFundName")}</label><input name="name" required placeholder="Car Maintenance"></div><div class="field"><label>${t("fieldReservedAmount")}</label><input name="amount" type="number" step="0.01" value="0"></div>`); if (type === "card" || type === "editCard") return form(`<input type="hidden" name="cardId" value="${escapeAttr(card.id || "")}"><div class="field"><label>${t("fieldCardName")}</label><input name="name" required placeholder="Citi Custom Cash" value="${escapeAttr(card.name || "")}"></div><div class="field"><label>${t("fieldBalanceOwed")}</label><input name="balance" type="number" step="0.01" value="${num(card.balance)}"></div><div class="field"><label>${t("fieldCreditLimit")}</label><input name="limit" type="number" step="0.01" value="${num(card.limit)}"></div><div class="field"><label>${t("fieldAvailOverride")}</label><input name="available" type="number" step="0.01" placeholder="Optional"></div><div class="field"><label>${t("fieldInterestRate")}</label><input name="apr" type="number" step="0.01" value="${num(card.apr)}"></div><p class="help-text">${t("fieldCardHelp")}</p>`); if (type === "expense") return form(`<div class="field"><label>${t("fieldAccountCard")}</label><select name="accountId">${accountOptions()}${cardOptions()}</select></div><div class="field"><label>${t("fieldMerchant")}</label><input name="merchant" required placeholder="Restaurant"></div><div class="field"><label>${t("fieldCategory")}</label><select name="category">${categoryOptions()}</select></div><div class="field"><label>${t("fieldAmount")}</label><input name="amount" type="number" step="0.01" required></div>`); if (type === "income") return form(`<div class="field"><label>${t("fieldDepositTo")}</label><select name="accountId">${accountOptions()}</select></div><div class="field"><label>${t("fieldSource")}</label><input name="merchant" required placeholder="Paycheck"></div><div class="field"><label>${t("fieldAmount")}</label><input name="amount" type="number" step="0.01" required></div>`); if (type === "adjustment") return form(`<div class="field"><label>${t("fieldAccount")}</label><select name="accountId">${accountOptions()}</select></div><div class="field"><label>${t("fieldNewBalance")}</label><input name="amount" type="number" step="0.01" required></div><div class="field"><label>${t("fieldNote")}</label><input name="merchant" placeholder="Reconcile balance"></div>`); if (type === "transfer") return form(`<div class="field"><label>${t("fieldFrom")}</label><select name="from">${accountOptions()}${fundOptions()}</select></div><div class="field"><label>${t("fieldTo")}</label><select name="to">${accountOptions()}${fundOptions()}</select></div><div class="field"><label>${t("fieldAmount")}</label><input name="amount" type="number" step="0.01" required></div>`); if (type === "payment") return form(`<div class="field"><label>${t("fieldFrom")}</label><select name="from">${accountOptions()}${fundOptions()}</select></div><div class="field"><label>${t("fieldCreditCard")}</label><select name="cardId">${cardOptions()}</select></div><div class="field"><label>${t("fieldAmount")}</label><input name="amount" type="number" step="0.01" required></div>`); if (type === "goal") return form(`<div class="field"><label>${t("fieldGoalName")}</label><input name="name" required placeholder="Emergency Fund"></div><div class="field"><label>${t("fieldTarget")}</label><input name="target" type="number" step="0.01" required></div><div class="field"><label>${t("fieldLinkToFund")}</label><select name="linkedFundId"><option value="">${t("fieldNone")}</option>${fundOptions()}</select></div>`); if (type === "institution") return form(`<div class="field"><label>${t("fieldName")}</label><input name="name" required placeholder="Fidelity"></div>`); if (type === "investmentAccount") return form(`<div class="field"><label>${t("fieldInstitution")}</label><select name="institutionId">${investmentInstitutionOptions(options.institutionId)}</select></div><div class="field"><label>${t("fieldAccountName")}</label><input name="name" required placeholder="Roth IRA"></div><div class="field"><label>${t("fieldAccountType")}</label><select name="accountType">${investmentAccountTypeOptions()}</select></div>`); if (type === "holding") return form(`<input type="hidden" name="holdingId" value="${escapeAttr(holding.id || "")}"><div class="field"><label>${t("fieldAccount")}</label><select name="accountId">${investmentAccountOptions(holding.accountId || options.investmentAccountId)}</select></div><div class="field"><label>${t("fieldTicker")}</label><input name="ticker" placeholder="VOO" value="${escapeAttr(holding.ticker || "")}"></div><div class="field"><label>${t("fieldHoldingName")}</label><input name="name" placeholder="Vanguard S&P 500 ETF" value="${escapeAttr(holding.name || "")}"></div><div class="field"><label>${t("fieldAssetType")}</label><select name="assetType">${assetTypeOptions(holding.assetType)}</select></div><div class="field"><label>${t("fieldValueMethod")}</label><select name="valueMode"><option value="shares" ${holding.valueMode !== "balance" ? "selected" : ""}>${t("sharesMethod")}</option><option value="balance" ${holding.valueMode === "balance" ? "selected" : ""}>${t("balanceMethod")}</option></select></div><div class="field"><label>${t("fieldShares")}</label><input name="shares" type="number" step="0.000001" value="${num(holding.shares)}"></div><div class="field"><label>${t("fieldPrice")}</label><input name="price" type="number" step="0.01" value="${num(holding.price)}"></div><div class="field"><label>${t("fieldBalance")}</label><input name="balance" type="number" step="0.01" value="${num(holding.balance)}"></div>`); if (type === "category") return form(`<div class="field"><label>${t("fieldCategoryName")}</label><input name="name" required placeholder="Pets"></div>`); return ""; }
  function form(body){ return `<form class="form-grid">${body}<button class="btn gold" type="submit">${t("saved")}</button></form>`; }

  function handleForm(type, fd) { const next = clone(); const amount = num(fd.get("amount")); try { if (type === "account") next.accounts.push({ id:id(), name:clean(fd.get("name")), type:fd.get("type"), balance:amount, funds:[] }); if (type === "fund") next.accounts.find((a)=>a.id===fd.get("accountId"))?.funds.push({ id:id(), name:clean(fd.get("name")), amount }); if (type === "card") upsertCard(next, fd); if (type === "editCard") upsertCard(next, fd, true); if (type === "expense") applyExpense(next, fd, amount); if (type === "income") applyIncome(next, fd, amount); if (type === "adjustment") applyAdjustment(next, fd, amount); if (type === "transfer") applyMove(next, fd.get("from"), fd.get("to"), amount, "transfer"); if (type === "payment") applyCardPayment(next, fd.get("from"), fd.get("cardId"), amount); if (type === "goal") { const parts=String(fd.get("linkedFundId")||"").split(":"); next.goals.push({ id:id(), name:clean(fd.get("name")), target:amount, linkedAccountId:parts.length===1?parts[0]:"", linkedFundId:parts[1]||"" }); } if (type === "category") next.categories.push(clean(fd.get("name"))); if (type === "institution") next.investmentInstitutions.push({ id:id(), name:clean(fd.get("name")) }); if (type === "investmentAccount") next.investmentAccounts.push({ id:id(), institutionId:fd.get("institutionId"), name:clean(fd.get("name")), accountType:fd.get("accountType") }); if (type === "holding") upsertHolding(next, fd); } catch (err) { toast(err && err.message ? err.message : "Could not save this transaction"); return; } save(next); closeModal(); toast(t("saved")); }
  function upsertCard(next, fd, edit) { const limit = num(fd.get("limit")); const availableRaw = clean(fd.get("available")); const balance = availableRaw ? Math.max(0, limit - num(availableRaw)) : num(fd.get("balance")); const payload = { name: clean(fd.get("name")), balance, limit, apr: num(fd.get("apr")) }; const existing = next.cards.find(c => c.id === fd.get("cardId")); if (edit && existing) Object.assign(existing, payload); else next.cards.push({ id:id(), ...payload }); }
  function upsertHolding(next, fd) { const accountId = fd.get("accountId"); if (!next.investmentAccounts.some((a) => a.id === accountId)) return; const existing = next.investmentHoldings.find((h) => h.id === fd.get("holdingId")); const payload = { accountId, ticker: clean(fd.get("ticker")).toUpperCase(), name: clean(fd.get("name")) || clean(fd.get("ticker")).toUpperCase() || "Holding", assetType: fd.get("assetType"), valueMode: fd.get("valueMode"), shares: Math.max(0, num(fd.get("shares"))), price: Math.max(0, num(fd.get("price"))), balance: Math.max(0, num(fd.get("balance"))), lastUpdated: existing?.lastUpdated || "" }; payload.value = holdingValue(payload); if (existing) Object.assign(existing, payload); else next.investmentHoldings.push({ id:id(), ...payload }); }
  function addTx(next, tx){ next.transactions.unshift({ id:id(), date:today(), ...tx }); }
  function parseRef(next, ref){ if(!ref) return null; if(String(ref).includes(":")){ const [accountId,fundId]=String(ref).split(":"); const account=next.accounts.find(a=>a.id===accountId); const fund=account?.funds.find(f=>f.id===fundId); return {kind:"fund",account,fund}; } const account=next.accounts.find(a=>a.id===ref); if(account) return {kind:"account",account}; return null; }
  function addToRef(next, ref, amount){ const r=parseRef(next,ref); if(!r)return; if(r.kind==="account") r.account.balance=num(r.account.balance)+amount; if(r.kind==="fund") r.fund.amount=num(r.fund.amount)+amount; }
  function refAvailable(ref){ if(ref.kind==="fund") return num(ref.fund.amount); return num(ref.account.balance)-accountReserved(ref.account); }
  function validRef(ref){ return !!(ref&&ref.account&&(ref.kind==="account"||(ref.kind==="fund"&&ref.fund))); }
  function requireTransferAmount(amount){ if(!Number.isFinite(amount)||amount<=0)throw new Error("Enter an amount greater than zero"); }
  function requireSourceFunds(ref,amount){ if(refAvailable(ref)+0.001<amount)throw new Error("The source does not have enough available money"); }
  function applyMove(next, from, to, amount, label){ requireTransferAmount(amount); if(String(from)===String(to))throw new Error("Choose two different locations"); const source=parseRef(next,from),target=parseRef(next,to); if(!validRef(source)||!validRef(target))throw new Error("Choose a valid source and destination"); requireSourceFunds(source,amount); if(source.kind==="account"&&target.kind==="account"){source.account.balance=num(source.account.balance)-amount;target.account.balance=num(target.account.balance)+amount;}else if(source.kind==="account"&&target.kind==="fund"){if(source.account.id!==target.account.id){source.account.balance=num(source.account.balance)-amount;target.account.balance=num(target.account.balance)+amount;}target.fund.amount=num(target.fund.amount)+amount;}else if(source.kind==="fund"&&target.kind==="account"){source.fund.amount=num(source.fund.amount)-amount;if(source.account.id!==target.account.id){source.account.balance=num(source.account.balance)-amount;target.account.balance=num(target.account.balance)+amount;}}else{source.fund.amount=num(source.fund.amount)-amount;target.fund.amount=num(target.fund.amount)+amount;if(source.account.id!==target.account.id){source.account.balance=num(source.account.balance)-amount;target.account.balance=num(target.account.balance)+amount;}}addTx(next,{type:label,amount,merchant:label,category:"Transfer"}); }
  function applyCardPayment(next,from,cardId,amount){ requireTransferAmount(amount); const source=parseRef(next,from),card=next.cards.find(c=>c.id===cardId); if(!validRef(source))throw new Error("Choose a valid payment source"); if(!card)throw new Error("Choose a valid credit card"); requireSourceFunds(source,amount); if(amount>num(card.balance)+0.001)throw new Error("Payment cannot exceed the card balance"); if(source.kind==="fund")source.fund.amount=num(source.fund.amount)-amount; source.account.balance=num(source.account.balance)-amount; card.balance=Math.max(0,num(card.balance)-amount); addTx(next,{type:"card payment",amount:-amount,merchant:"Credit card payment",category:"Transfer"}); }
  function applyExpense(next, fd, amount){ const ref=fd.get("accountId"); const card=next.cards.find(c=>c.id===ref); if(card) card.balance=num(card.balance)+amount; else addToRef(next, ref, -amount); addTx(next,{type:"expense",amount:-amount,merchant:clean(fd.get("merchant")),category:fd.get("category")}); }
  function applyIncome(next, fd, amount){ addToRef(next, fd.get("accountId"), amount); addTx(next,{type:"income",amount,merchant:clean(fd.get("merchant")),category:"Income"}); }
  function applyAdjustment(next, fd, amount){ const a=next.accounts.find(x=>x.id===fd.get("accountId")); if(a){ const diff=amount-num(a.balance); a.balance=amount; addTx(next,{type:"adjustment",amount:diff,merchant:clean(fd.get("merchant"))||"Balance adjustment",category:"Adjustment"}); } }
  async function lookupPrice(holding) { const symbol = clean(holding.ticker).toUpperCase(); if (!symbol || holding.valueMode === "balance") return null; if (holding.assetType === "Crypto") { const ids = { BTC:"bitcoin", ETH:"ethereum", XRP:"ripple" }; const coin = ids[symbol] || symbol.toLowerCase(); const res = await fetch(`https://api.coingecko.com/api/v3/simple/price?ids=${encodeURIComponent(coin)}&vs_currencies=usd`); const json = await res.json(); return json?.[coin]?.usd ? num(json[coin].usd) : null; } const stooq = symbol.replace(".", "-").toLowerCase() + ".us"; const res = await fetch(`https://stooq.com/q/l/?s=${encodeURIComponent(stooq)}&f=sd2t2ohlcv&h&e=csv`); const text = await res.text(); const line = text.trim().split(/\r?\n/)[1]; const close = line ? line.split(",")[6] : ""; return close && close !== "N/D" ? num(close) : null; }
  async function refreshInvestmentPrices(silent) { const next = clone(); let updated = 0; const stamp = new Date().toISOString(); for (const holding of next.investmentHoldings) { try { const price = await lookupPrice(holding); if (price) { holding.price = price; holding.lastUpdated = stamp; updated++; } holding.value = holdingValue(holding); } catch (err) { console.warn("Price refresh failed", holding.ticker, err); holding.value = holdingValue(holding); } } next.settings.lastInvestmentRefresh = stamp; save(next); if (!silent) toast(updated ? `${updated} ${t(updated === 1 ? "priceUpdated1" : "priceUpdatedN")}` : t("noPricesUpdated")); }
  function renderOnboarding(){ const el=$("onboarding"); el.classList.remove("hidden"); el.innerHTML=`<div class="modal-card"><div class="eyebrow">${t("firstTimeSetup")}</div><h2>${t("welcome")}</h2><p class="help-text" style="margin:8px 0 16px">${t("setupCopy")}</p><form id="setup-form" class="form-grid"><div class="field"><label>${t("firstName")}</label><input name="name" required placeholder="David"></div><button class="btn gold">${t("start")}</button><button type="button" class="btn secondary" id="setup-skip">${t("skip")}</button></form></div>`; $("setup-form").onsubmit=(e)=>{e.preventDefault(); const next=clone(); next.settings.firstName=clean(new FormData(e.target).get("name")); next.setupComplete=true; save(next); el.classList.add("hidden");}; $("setup-skip").onclick=()=>{const next=clone(); next.setupComplete=true; save(next); el.classList.add("hidden");}; }
  function help(topic){ const copy={ dashboard:t("helpDashboard"), banking:t("helpBanking"), cards:t("helpCards"), transactions:t("helpTransactions"), transfers:t("helpTransfers"), goals:t("helpGoals"), investments:t("helpInvestments")}; const modal=$("modal"); modal.innerHTML=`<div class="modal-card"><div class="section-title"><h2>${t("howThisWorks")}</h2><button class="icon-btn" data-close>×</button></div><p class="help-text">${copy[topic]||copy.dashboard}</p></div>`; modal.classList.remove("hidden"); modal.querySelector("[data-close]").onclick=closeModal; }

  applyTheme(); render(); if (state.investmentHoldings.some((h) => h.ticker && h.valueMode !== "balance")) refreshInvestmentPrices(true); if("serviceWorker" in navigator) navigator.serviceWorker.register("sw.js").catch(()=>{});
})();
